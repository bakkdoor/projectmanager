<div class="title">
  <h1>
    Alle Arbeitszeiten
  </h1>
</div>

<% Project.all.each do |project| %>
  <div class="subtitle">
    <strong>Arbeitszeiten für Projekt: <%= project.name %></strong>
  </div>
  <%= render :partial => "worktimes_list", :locals => {:worktimes => project.worktimes} %>
  <br/>
<% end %>
<br />
