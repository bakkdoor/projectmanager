| <% if logged_in? -%>
  Eingeloggt als <%= link_to current_user.login, profile_url %>
  (<%= link_to "#{icon_tag('door_out')} Log out", logout_path, { :title => "Log out" }    %>)
<% else -%>
  <%= link_to "#{icon_tag('door_in')} Log in",  login_path,  { :title => "Log in" } %>
   / <%= link_to "#{icon_tag('user_add')} Sign up", signup_path, { :title => "Create an account" } %>
<% end -%>
